#include<stdio.h>
#include<string.h>
int main()
{
	char *s;
	int len,i;
	printf("\n Enter the string : ");
	gets(s;
	printf("Reverse string : ");
	len = strlen(s);
	for(i=len;i>0;i--)
	{
		printf("%c",*(s+i));
	}
	return 0;
}
